Defines interfaces for services that have distinct implementations for different platforms.

Services must be declared in each platform's module, then the fully qualified class name added to META-INF.

They can then be discovered by the PlatformHelper.