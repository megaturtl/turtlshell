package cc.turtl.turtlshell

import cc.turtl.turtlshell.BuildDetails.MOD_ID
import org.apache.logging.log4j.LogManager
import org.apache.logging.log4j.Logger

object TurtlShell {
    val LOGGER: Logger = LogManager.getLogger(MOD_ID)

    init {

    }
}